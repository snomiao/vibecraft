---
kanban-plugin: board
---

## Long term

- [ ] support file editor
- [ ] capture images and send to ai
- [ ] map hotkeys for hopping around the map
- [ ] clean up servers and other background processes initiated by the AI when the agent is killed
- [ ] make the agent respond in chat when it's done with a summary of what it did
- [ ] hotkeys editor in settings
- [ ] the settings page should be fun to play with too, it should have a spatial design where information is organised neatly

## To Do

- [ ] modify the thinking animation so instead of dot dot dots, it becomes like steam puffing up, or clouds, like a thought bubble. so it looks like there's steam expanding out from the agent unit.

## Done

- [ ] BUG units stop orbiting when creating a fork (worktree)
- [ ] remove the ability to delete claude code and codex buildings
- [ ] remove the move ability on the hero
- [ ] remove the manage workspace ability on the hero
- [ ] I want to kill multiple agents at a time with the delete hotkey (already works with ability). so if I have many agents selected, then pressing the delete hotkey should kill all of them
- [ ] make a new terminal building which is a building similar to the browser and existing terminal display for agents, but one which is just the normal terminal where users can enter commands and etc. terminal buildings should be summoned by folder buildings, so folder buildings will have an ability to create a terminal building, and the terminal building will have the folder as its working directory.
- [ ] improve Codex integration for non‑interactive/CI‑style workflows.
- [ ] I want to add a feature to resize browsers and terminals by having corner outposts which you can move. the corners of these buildings have a mini building which you can drag with the same drag‑to‑move interaction. these corner outposts should look like those of a castle, and the "walls" (the edges of the browser frame) should look like castle walls too, but not be too thick, they shouldn't take up too much additional ui space. so each building which can be resized (browser and terminal) should have outposts in the corners which get moved by dragging to resize them.

%% kanban:settings

```
{"kanban-plugin":"board","list-collapse":[false,false,false]}
```

%%
